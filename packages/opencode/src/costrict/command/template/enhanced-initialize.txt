# 代码仓库深度分析和AGENTS.md文档生成

## 任务目标
您是一位技术文档撰写专家，精通代码分析、架构解构与技术写作。
你的任务是深度分析本代码库的核心内容（包括目录结构、代码文件、配置文件、测试用例、提交记录、现有文档等），生成一份高质量、精细化的 AGENTS.md 文件。
该文件专供在本代码仓库内运行的 AI Coding Agent 使用，核心目标是：
1. 成为 Agent 处理本仓库相关任务的「唯一权威参考」，精准理解仓库的设计理念、实现细节和维护标准；
2. 确保 Agent 输出的代码/解决方案完全契合本仓库的架构设计、技术风格和业务逻辑，避免与现有代码冲突；
3. 支持 Agent 高效完成设计、编码、调试、重构等核心软件工程任务。

## 输入参数
arguments: $ARGUMENTS

注: 如果没有输入参数，忽略此参数。

## 输出文件

${path}AGENTS.md

## 📋 详细执行步骤

###执行要点（全局通用，指导所有子任务及父Agent任务）
1. 子任务委托与执行规范
  - 子任务委托：使用`task`工具将各子任务委派给subagent，避免上下文冗余累积，提升执行效率；
  - 串行执行原则：所有子任务需逐个串行执行，完成一个子任务并确认达标后，再启动下一个，确保任务衔接无遗漏；
  - 并行工具调用：只读类操作（如读取文件、检索git记录）可并行执行，单次消息中工具调用不超过10个，兼顾效率与稳定性。

2. 文件操作约束（严格遵循，杜绝违规操作）
  - 子Agent写操作：每个子Agent直接向${path}AGENTS.md文件写入内容，禁止向父Agent返回大量冗余内容；
  - 忽略目录要求：统一跳过常见隐藏/无关目录，包括但不限于.vscode、node_modules、dist、vendor、build等；
  - 唯一可编辑文件：全程仅可编辑${path}AGENTS.md文件，禁止操作任何其他文件或目录；
  - 路径规范：所有文件引用均使用相对项目根目录的相对路径，格式为[文件相对路径:行号]，禁止使用绝对路径。

3. 子任务上下文与质量要求
  - 上下文传递：给子Agent的输入信息需完整、精准，包含完成任务所需的全部关键信息，确保子Agent无需额外追问即可高效执行；
  - 核心原则：所有子任务执行需遵循“实事求是、简洁高效、质量优先”，结论必须基于项目真实信息，禁止猜测、虚构内容；
  - 增量更新：子Agent仅更新 ${path}AGENTS.md 中与自身任务相关的内容，不重复输出已存在的达标内容，避免文档冗余。

4. 子任务模板
```json
{
  "subagent_type": "general",
  "description": "{任务简短描述}",
  "prompt": "
    **{子任务名称}**
    ## 任务目标
      {明确子任务核心目标，简洁具体，贴合整体项目指南生成需求}
    ## 任务指令
      {任务详细执行步骤，后面定义的具体子任务的**任务指令**部分}
    ## 核心原则
      1.  实事求是: 从真实代码中学习，所有结论都必须基于项目中真实存在的信息（目录、文件、代码、提交记录等），禁止猜测、虚构。
      2.  保持简洁: 只输出关键信息和参考位置，避免重复、冗余信息，避免大量代码片段。
      3.  并行工具调用：并行进行读取、检索等只读类工具调用（在一次消息中包含多个工具调用，但不要超过10个），提高效率。
      4.  路径引用: 参考文件以相对路径引用，格式为 [文件相对路径:行号]。使用相对项目根目录的相对路径，不要使用绝对路径。
      5.  增量更新: 只更新 ${path}AGENTS.md 文件中的与任务相关的内容，不要重复输出已有的内容。
      6.  唯一可编辑：你唯一能编辑的文件只有 ${path}AGENTS.md 文件，禁止操作其他文件或与本任务不相关的章节。
      7.  质量优先：重点关注对AI Coding有价值的、能帮助 AI 学习的内容，优先保留核心信息。
      8.  语言偏好：如果用户未指定，则输出语言应为**简体中文**。
      9.  {其他注意事项：各个具体子任务定义的**注意事项**部分}
    ## 输入参数（如有，没有则跳过此项）
      {输入参数，无则删除本章节}
    ## 输出要求
      - 输出路径：${path}AGENTS.md
      - 输出格式:
      ```
      {本子任务相关的输出示例、格式，需对齐各个具体子任务**输出示例**对应章节格式}
      ```
  "
}
```
注：模板中所有`{}`占位符需替换为实际内容，无对应内容的章节（如输入参数）可直接删除，禁止保留占位符或空章节；json格式需严格规范，避免语法错误。


### 子任务1：项目基本信息及AGENTS.md文件初始化
####任务指令
  1. 读取已有AGENTS.md文件（如有），了解现状。
  2. 快速分析项目，获取如下信息：	
    - 项目名称（从 git 或目录名获取）
	  - 项目简介（从 README 或代码注释提取）
	  - 主要编程语言（分析文件扩展名分布）
	  - 核心框架/技术栈（从依赖文件识别）
  3. 获取当前分支、最新git commit id （如果）和当前时间。
  4. 将上述信息输出到AGENTS.md文件中。
  5. 确保AGENTS.md文件存在，不存在则创建。
  6. 按照示例及格式要求，将内容输出到AGENTS.md文件中。

####输出格式
```
## 项目信息
- 项目名称：{name}
- 项目简介：{一句话描述，100字以内}
- 主要语言：{主要语言及版本}
- 核心框架：{核心框架及版本}
- 分支：{当前分支，如有}
- commit: `{git commit id，如有}`
- 生成时间： {当前时间，示例：2023-09-01 12:00:00}
```

### 子任务2：项目结构分析
#### 任务指令（按执行顺序）：
  1. 读取已有AGENTS.md文件，了解现状。
  2. 快速分析项目，识别如下信息：	
  - 项目关键目录结构树（跳过常见忽略目录）
	- 项目核心模块
	- 项目公共模块
  3. 分析项目近6个月的Git提交历史：
  - 仅提取文件修改记录（无需具体修改内容）
  - 按“目录/文件的修改次数”从高到低排序
  - 识别高频修改的目录、高频修改的代码文件
  4. 按以下优先级排序规则，筛选需展示的目录/文件及展示深度（整体展示内容需满足行数限制）：
    优先级从高到低：
   ① 根目录下的所有目录及核心文件
   ② 近6个月高频修改的目录
   ③ 项目核心模块对应的目录
   ④ 项目公共模块对应的目录
   ⑤ 项目入口文件、全局配置文件、核心文档、通用脚本
   ⑥ 近6个月高频修改的代码文件
   ⑦ 核心模块下的关键代码文件
   ⑧ 公共模块下的关键代码文件
   ⑨ 其他非核心目录
   ⑩ 其他非核心文件
  5. 对筛选后需展示的目录，读取目录内2-3个核心文件的关键信息（如文件功能、依赖关系），用于说明该目录的业务功能、架构职责等；
  6. 按照“输出格式”要求，将分析结果结构化更新至 ${path}AGENTS.md 文件的相关章节中（无章节则新增）。

#### 注意事项：
##### 遍历规则
- 采用“广度优先”遍历方式，从外层目录向内层目录逐层递进展示；
- 跳过常见隐藏目录/文件：.vscode、.git、node_modules、vendor、dist、build等；

##### 格式限制
- 最终输出的目录结构树总行数≤100行；
- 每条目录/文件的注释描述≤30字，仅说明核心功能/关联业务，避免冗余；
- 小型项目展示至文件级别，中大型项目按优先级选择性展示至“目录+核心文件”层级。

##### 折叠规则
- 非关键目录/文件统一用`...`折叠，折叠后需附带简短说明（≤15字），明确折叠内容类型、功能描述等；
- 优先折叠低优先级的目录/文件，确保高优先级内容完整展示。

####输出格式
```
## 项目结构
### 核心目录
{核心目录结构树和描述信息}
\`\`\`
prject_root_name/
├─ src/                # 核心模块：业务逻辑
│  ├─ api/             # 接口层：接收HTTP请求，关联业务：用户下单
│  │  ├─ user_api.py   # 用户接口处理，依赖src/service/user.py）
│  │  └─ ...           # 其它接口，如：订单、商品
│  └─ service/         # 服务层：处理业务逻辑
└─ config/             # 配置区：全局参数设置
└─ main.py             # 程序入口，初始化应用环境，关联业务：系统启动
└─ requirements.txt    # 依赖列表
\`\`\`
```


### 子任务3：项目规范挖掘
####任务指令
  1. 读取已有AGENTS.md文件，了解现状。
  2.结合子任务2识别的核心模块文件、高频修改文件、公共模块文件，深度分析以下内容，挖掘项目潜在规范：
  - 项目代码分层职责（与子任务2目录结构对应，明确各层级核心作用）；
  - 代码存放规则（明确不同类型代码对应的目录位置，呼应子任务2目录优先级）；
  - 命名规范（含文件、函数、类、方法、变量，明确命名格式、大小写等）；
  - 日志、异常、错误处理规范（含处理流程、输出格式、存放位置）；
  - 常用公共库和工具函数（含使用场景、核心文件路径，无需代码示例）；
  - 配置文件管理规范（含配置分类、存放路径、修改规则）；
  - 测试规范（含测试框架、测试文件组织方式、核心测试代码模式）；
  - 项目常用命令（含构建、测试、启动、部署等）。
  3. 总结编码、测试规范：以编码规范为主，整体不超过30行，每条规范简洁精炼、有约束性，无冗余表述；
  4. 分析项目近6个月的Git提交历史：
  - 从仓库最近的代码变更中，学习这个项目的真实编码方式，提取有价值的编码实践；
  - 挖掘高频编码任务，聚焦高价值、高频次开发任务（如新增接口、修复核心bug），明确每个任务的代码位置和注意事项，形成快速导航；
  5. 严格遵循本提示词“输出格式”，将上述所有信息更新至AGENTS.md文件，仅替换/新增“开发规范、常用命令、快速导航”章节，保留文件原有其他内容。

####注意事项
  1. 分析约束
  - 核心代码文件分析数量≥30个，优先选择子任务2识别的高频修改文件、核心模块文件、公共模块文件；
  - 分析范围需覆盖：核心代码、配置文件、脚本、测试代码、文档及近6个月git提交记录（重点关联修改频率高的内容）。
  2. 规范约束
  - 编码、测试规范总条数不超过30行，每条规范≤30字（日志、公共库相关可放宽至50字）；
  - 禁止包含任何代码示例，仅标注参考代码文件路径（格式：`目录/文件名`），不展开具体代码内容；
  - 规范内容需具备强指导性和约束性，明确“必须做/禁止做/怎么做”，避免模糊表述（如不说“尽量规范命名”，说“变量需用小写下划线命名，禁止驼峰”）。
  3. 快速导航约束
  - 导航任务总条数≤20条，优先选择近6个月git修改频繁、核心业务相关的高价值任务；
  - 每个任务需明确“任务描述、代码位置、注意事项”，三者对应一致，贴合项目实际开发场景；
  - 任务描述≤20字，代码位置标注精准目录/文件路径，注意事项≤50字（聚焦关键要点，无冗余）。
  4. 常用命令需筛选项目实际开发中高频使用的，无需罗列所有命令，标注命令用途便于理解。
  5. 所有输出内容需控制冗余，严格遵循字数、条数限制，确保AGENTS.md文件简洁、实用。

####输出格式
```
## 开发规范
### 命名规范
- {文件、函数、类、方法、变量命名规范，每条≤30字，明确约束}
#### 日志&异常&错误处理
- {规范内容+参考文件路径，每条≤50字，无代码示例}
#### 公共库和工具函数
- {使用场景+核心文件路径，每条≤50字，明确适用范围}
#### 配置文件管理规范
- {配置分类/存放/修改规则，每条≤30字，有约束性}
#### 测试规范
- {测试框架/文件组织/模式，每条≤30字，聚焦核心要求}

### 常用命令
```bash
# 构建相关（标注命令用途，简洁明了）
{构建相关命令}
# 测试相关
{测试相关命令}
# 启动/部署相关（按需补充，贴合项目实际）
{启动/部署等其他常用命令}
```

### 快速导航
| 任务 | 位置 | 注意事项 |
|------|----------|----------|
| {可预见的高频任务描述，≤20字，贴合实际开发} | `{关联目录/文件路径，精准标注}` | {完成该任务的关键注意事项，≤50字，有指导性} |
| {可按任务频率排序，优先高频高价值任务} | `{路径格式统一，与子任务2目录呼应}` | {聚焦核心要点，避免冗余} |
```


### 任务4：固定内容添加
注：本任务在父Agent中执行，无需委派给子Agent。
####任务指令
  1. 读取已有AGENTS.md文件，梳理文件现有内容，明确是否已存在结尾“通用约束”固定部分；
  2. 根据最终输出示例中的内容，将“通用约束”固定部分添加至AGENTS.md文件末尾，若该部分已存在且完全一致，切勿重复添加。

### 任务5：审查校验
注：本任务在父Agent中执行，无需委派给子Agent。
####任务指令
  1. 读取最终版本的AGENTS.md文件，梳理文档完整内容，明确当前文档结构、格式及内容细节，对照输出格式及子任务要求，排查现状问题；
  2. 结合最终输出示例、项目实际内容，对AGENTS.md文件进行全面、深度审查，逐一校验所有审查项；
  3. 针对审查中发现的不符合项，立即进行修正（补充缺失内容、修正格式错误、更正路径/行号、删除无关内容等），确保文档完全符合要求；
  4. 修正完成后，再次复核一遍，确认所有审查项均达标、内容完整真实、无遗漏修正，最终固化AGENTS.md文件版本。
####审查清单
  1. 文档完整性审查
  - 文档是否完整包含所有必填章节（项目结构、开发规范、常用命令、快速导航、通用约束），缺少则补充完整；
  - 各章节核心内容是否齐全（如开发规范各子项、快速导航任务条目），无关键信息缺失。

  2. 格式合规性审查
  - 文档整体格式、各章节层级是否严格遵循前文“输出格式”要求，无层级混乱、格式错误；
  - 代码块、表格、路径标注（`目录/文件名`）等格式是否规范，无语法错误；
  - 目录结构树、规范条目、导航表格等是否符合字数、条数限制（如目录≤100行）。

  3. 内容准确性审查
  - 文档中所有文件路径、行号是否准确，与项目实际目录、子任务2识别结果一致，存在错误则修正；
  - 是否包含虚构、主观臆测、错误或不准确的内容（如虚假路径、错误规范），是则彻底修正；
  - 是否包含与项目无关的冗余内容（如无关命令、多余规范），是则精准删减，保留核心内容。
  4. 指导性与篇幅审查
  - 文档内容（尤其是规范、快速导航、常用命令）是否对AI Coding具备强指导性，否则优化调整；
  - 文档总行数是否符合要求，若超过限制，优先删减低价值、冗余内容，确保不超标且核心信息不缺失。

####注意事项
  - 审查需全面无死角，每个审查项逐一校验，不遗漏任何细节（如路径拼写、格式缩进、字数限制）；
  - 修正需遵循“最小改动”原则，仅修正不符合项，不随意修改符合要求的核心内容、不新增无关信息；
  - 复核环节不可省略，确保修正后无新问题，且与子任务2、3的输出内容保持一致，无逻辑冲突；
  - 审查、修正过程无需记录，仅输出最终固化的AGENTS.md文件即可。
---


## 输出示例

AGENTS.md:
```markdown
# 项目开发指南

## 项目信息
- 项目名称：{name}
- 项目简介：{一句话描述，100字以内}
- 主要语言：{主要语言及版本}
- 核心框架：{核心框架及版本}
- 分支：{当前分支，如有}
- commit: `{git commit id，如有}`
- 生成时间： {当前时间，格式为：yyyy-mm-dd HH:MM:SS}

## 项目结构

\`\`\`
prject_root_name/
├─ src/                # 核心模块：业务逻辑
│  ├─ api/             # 接口层：接收HTTP请求，关联业务：用户下单
│  │  ├─ user_api.py   # 用户接口处理，依赖src/service/user.py
│  │  └─ ...           # 其它接口，如：订单、商品
│  └─ service/         # 服务层：处理业务逻辑
└─ config/             # 配置区：全局参数设置
└─ main.py             # 程序入口，初始化应用环境，关联业务：系统启动
└─ requirements.txt    # 依赖列表
\`\`\`

## 开发规范
### 命名规范
- 文件命名：小写连字符式，如user-service.ts，类型文件加.d.ts后缀
- 函数/方法：小驼峰式，动词开头，如getUserInfo，避免无意义命名
- 类名：大驼峰式，名词开头，如UserService，体现职责单一
- 变量：小驼峰式，语义明确，如userList而非data1/data2
- 常量：全大写下划线分隔，如MAX_RETRY_COUNT，声明为const

#### 日志&异常&错误处理
- 复用src/utils/logger.ts，所有异常通过该模块记录日志
- 日志分级调用，如logger.error()，错误码定义src/constants/error.ts
- 异步错误捕获后，调用src/utils/error-handler.ts统一处理

#### 公共库和工具函数
- 优先复用src/utils/目录下工具函数，如src/utils/validate.ts
- 第三方库封装到src/libs/，如src/libs/axios.ts统一请求
- 工具函数单测文件放src/utils/__tests__/，如validate.test.ts

#### 配置文件管理规范
- 环境配置放src/config/env.ts，区分dev/prod环境
- 敏感配置读取src/config/secrets.ts，不硬编码到业务文件
- 模块配置拆分，如src/config/database.ts、src/config/app.ts

#### 测试规范
- 单测文件与源码同目录，如src/services/user-service.test.ts
- 集成测试放src/__tests__/integration/，如user-api.test.ts
- 测试用例数据放src/__tests__/mocks/user-mock.ts

### 相关命令
```
bun run typecheck      # Type check
bun run build          # ESM + declarations + schema
bun run rebuild        # Clean + Build
bun run build:schema   # Schema only
bun test               # Run tests (76 test files, 2559+ BDD assertions)
```

### 快速导航
| 任务 | 位置 | 注意事项 |
|------|----------|-------|
| 添加agent | `src/agents/` | 创建 .ts, 添加到index.ts 的builtinAgents函数中 , 再更新 types.ts |
| 添加hook | `src/hooks/` | 使用 createXXXHook() 创建目录, 从 index.ts 中导出 |

## 通用约束
- 设计与编码严格遵循以下核心原则：
  - KISS 原则：聚焦简洁性，拒绝冗余复杂逻辑；
  - DRY 原则：聚焦复用性，禁止重复编写同类代码；
  - 单一职责原则：聚焦职责边界，类 / 函数仅负责一项核心功能；
  - 单一数据源原则：聚焦数据一致性，同一业务数据仅从可信源获取；
  - 最小侵入原则：聚焦兼容性，新增功能不改动原有核心逻辑；
- 优先复用/借鉴项目现有同类机制、工具类及业务逻辑，禁止无意义重复造轮子。
- 严格对齐项目既有编码模式，包括异常处理、日志输出、配置管理及接口调用规范。
- 禁止编写冗余无效代码、未使用变量，杜绝复述代码功能的冗余注释。
- 禁止过度设计，仅实现当前需求明确功能，不增设无业务依据的扩展代码。
- 代码交付前必须全面自检，确保无语法、路径、依赖错误，且符合本清单所有约束。
```

注: 
1. 输出示例中的占位符{xxx}应替换为实际内容，或在不适用时忽略。
2. 输出示例中[xxx]的内容为指导信息，仅用于指导输出，实际输出中不要包含。
3. 示例为标准模板，可根据项目实际规模（小型/中大型）灵活调整展示粒度，核心是满足“行数限制、约束要求、格式规范”。

## 注意事项
1. 如果存在 Cursor 规则（位于 .cursor/rules/ 或 .cursorrules 中）或 Copilot 规则（位于 .github/copilot-instructions.md 中），请确保包含它们。
2. 如果已存在 AGENTS.md 文件，且其位于 ${path} 路径下，请对其进行改进。
3. 确保 AGENTS.md 文件的行数在300 行以内。
4. 如果用户未指定，则输出语言应为**简体中文**。

现在，请开始按照**执行步骤**的定义的任务严格执行，深度、全面分析本代码仓库，最终生成一个符合要求的AGENTS.md文件。
